{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Connexion list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Createdat</th>
                <th>Source</th>
                <th>Destination</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_connexion_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.createdAt|date('Y-m-d H:i') }}</td>
                <td>{{ entity.source }}</td>
                <td>{{ entity.destination }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_connexion_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_connexion_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_connexion_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
