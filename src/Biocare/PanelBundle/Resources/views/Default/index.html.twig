{% extends '::base.html.twig' %}

{% block body -%}
    <div class='container'>
        <div class='row'>
            <div class='col-lg-12'>
                It's  {{ callregister.created|date('Y-m-d H:i') }}!
                Hello {{ callregister.createdBy }}!
                You are logged on {{ callregister.createdFromIp }}!
            </div>
        </div>
    </div>
{% endblock %}
