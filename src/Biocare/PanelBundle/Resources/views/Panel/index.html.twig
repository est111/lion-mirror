{% extends '::base.html.twig' %}

{% block body -%}
    <div class='container'>
        <div class='row'>
            <div class='col-lg-4'>
                <div class="panel panel-primary">
                    <div class="panel-heading">
                        <strong>Debug</strong>
                    </div>
                    <div class="panel-body">
                        It's all about call at <strong>{{ callregister.created|date('Y-m-d H:i') }}</strong> !<br/>
                        Hello <strong>{{ callregister.createdBy }}</strong> !<br/>
                        You are logged on <strong>{{ callregister.createdFromIp }}</strong> !<br/>
                    </div>
                </div>
            </div>
            <div class='col-lg-8'>
            </div>
        </div>
    </div>
{% endblock %}
