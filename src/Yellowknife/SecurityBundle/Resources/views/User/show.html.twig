{% extends '::base.html.twig' %}

{% block body -%}
    <div class="container-fluid bg-danger">
        <div class="row">
            <div class="col-lg-12">
              
                <h2>{{ entity.firstname }} {{ entity.lastname }} <small>{{ entity.username }}  #{{ entity.id }}</small></h2>
                    
                           
               
                           {{ entity.password }}
                
                           {{ entity.email }}
        
                           {{ entity.locale }}
          
                           {{ entity.isActive }}
                           
            </div>
        </div>
    </div>
{% endblock %}
